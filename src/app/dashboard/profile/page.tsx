import EmployeesProfile from "@/components/dashboard/employees/EmployeeProfile";
import { Wrapper } from "@/components/utils/Wrapper";

const Home = () => {
  return (
    <Wrapper>
      <EmployeesProfile />
    </Wrapper>
  );
};

export default Home;
